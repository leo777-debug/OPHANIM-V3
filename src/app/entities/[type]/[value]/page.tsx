import { entityLink } from '@/lib/links';
export default async function EntityPage({params}:{params:Promise<{type:string;value:string}>}){const {type,value}=await params;const decoded=decodeURIComponent(value);return <main className="min-h-screen bg-[#020707] p-8 text-white"><p className="text-xs text-emerald-300">OPHANIM ENTITY</p><h1 className="mt-2 text-3xl">{decoded}</h1><p className="mt-2 text-zinc-400">{decodeURIComponent(type)}</p><a className="mt-6 inline-block text-emerald-300 underline" href={`/?q=${encodeURIComponent(decoded)}`}>Investigate in Ophanim</a><p className="mt-6 text-xs text-zinc-500">Stable link: {entityLink(type,decoded)}</p></main>}
