import { incidentLink } from '@/lib/links';
export default async function IncidentPage({params}:{params:Promise<{source:string;id:string}>}){const {source,id}=await params;return <main className="min-h-screen bg-[#020707] p-8 text-white"><p className="text-xs text-emerald-300">OPHANIM INCIDENT</p><h1 className="mt-2 text-3xl">{decodeURIComponent(source)}</h1><p className="mt-2 text-zinc-400">Incident {decodeURIComponent(id)}</p><a className="mt-6 inline-block text-emerald-300 underline" href="/">Open live map</a><p className="mt-6 text-xs text-zinc-500">Stable link: {incidentLink(source,id)}</p></main>}
